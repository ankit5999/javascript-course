//////// Arrow function

const even = (num) => {
    return num % 2 === 0;
}

console.log(even(6));


//// or

const birthday = song => song; // can use if we have only one statement in it
console.log(birthday("Happy Birthday ... "));


/// or

const sum = (num1, num2) => num1+num2;
console.log(sum(3,2));